# BloodMoon-Reborn - CommandsOnEnd examples
#
# Choose and merge ONLY ONE version into each world config.yml:
# - CommandsOnEnd-en.yml (English visible messages)
# - CommandsOnEnd-es.yml (mensajes visibles en español)
#
# Each variant includes a final announcement, a global reward, console
# commands, and a survivor message. Do not merge both lists.
#
# Cada variante incluye anuncio final, recompensa global, comandos de consola
# y mensaje para supervivientes. No combine ambas listas.
