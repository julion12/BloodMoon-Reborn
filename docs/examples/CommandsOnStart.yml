# BloodMoon-Reborn - CommandsOnStart examples
#
# Choose and merge ONLY ONE version into each world config.yml:
# - CommandsOnStart-en.yml (English visible messages)
# - CommandsOnStart-es.yml (mensajes visibles en español)
#
# Do not merge both lists. Command names, selectors, placeholders, suffixes,
# and technical syntax are intentionally identical in both variants.
#
# Seleccione y combine SOLO UNA versión con el config.yml de cada mundo.
# No combine ambas listas.
