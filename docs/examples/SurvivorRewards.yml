# BloodMoon-Reborn - SurvivorRewards examples
#
# Choose and merge ONLY ONE version into each world config.yml:
# - SurvivorRewards-en.yml (English visible text)
# - SurvivorRewards-es.yml (texto visible en español)
#
# Do not merge both sections. YAML keys, Bukkit materials, commands,
# placeholders, and internal modes remain unchanged.
#
# Seleccione y combine SOLO UNA versión. Las claves YAML, materiales Bukkit,
# comandos, placeholders y modos internos no se traducen.
