# BloodMoon-Reborn - BossRewards examples
#
# Choose and merge ONLY ONE version into each world config.yml:
# - BossRewards-en.yml (English visible text)
# - BossRewards-es.yml (texto visible en español)
#
# Do not merge both sections. MythicMobs IDs, materials, commands,
# placeholders, YAML keys, and internal modes remain unchanged.
#
# Seleccione y combine SOLO UNA versión. Los IDs de MythicMobs, materiales,
# comandos, placeholders, claves YAML y modos internos no se traducen.
